import React, {useState} from 'react'

const NotionCards:React.FC<NotionCard>=()=>{
    const [active,setActive] =useState(false);

    return <div className="flex">

    </div>
}

export default NotionCards
